; This config file just uses example values, some of which (usernames/passwords) you will have to replace yourself to fit the environment.
; You will also have to rename this file to 'testconfig.ini' for the tests to detect it properly.
test_hostname=localhost		                ;name (IP) of the test host to be accessed (probably just localhost)
test_database_name=iefdf		            ;name of the test database to be accessed (probably just hige)
test_database_username="exampleDBusername" 	;test database username
test_database_password="exampleDBpassword"	;test database password